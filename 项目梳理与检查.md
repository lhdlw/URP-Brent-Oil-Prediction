# 项目梳理与上传前检查

检查日期：2026-08-31。初次检查范围：现有源代码静态检查、结题报告正文、部分数据表头与历史结果文件。随后应用户要求，使用现有环境运行了三/四因子 VIP 分析和四模型综合对比；未安装依赖、未改写原代码或历史结果。

## 第一阶段修复状态

已按用户要求修复 `核心代码/模型综合对比.py`：每次模型训练分别创建 `EarlyStopping`，保留原 patience=20、最佳权重恢复、模型结构、训练参数与数据处理。

已在本次运行环境中完成独立回调验证：首模型最佳损失 0.1 后，后三组分别从 1.0 持续改善至 0.52，均能通过 25 轮模拟回调、不被错误停止，并恢复各自第 25 轮权重。语法解析和四个训练入口检查通过。此验证仅检查回调行为，不是完整模型重训；此前预测指标仍属于修改前版本。

修改前代码、差异和验证记录位于 `模型输出/提前停止修复_20260831/`。归一化泄漏、周频插值、OPEC 字段等问题尚未改动，后续应分阶段修正。尚未上传 GitHub。

## 后续实际运行结果（修复前版本）

两组 VIP 分析和四模型综合对比均已跑完，新结果独立保存于 `模型输出/复核运行_20260831/`，详见其中的 `运行说明.md`、`summary.json` 和日志。

- 五份实际输入各 1,096 行，均为周频记录（2005-01-02 至 2025-12-28），原综合对比将其插值为日频；报告“日度数据”的表述需要修正或说明。
- OPEC 全部行的 OHLC 四列相等，确认不是仅首行出现的现象。
- 四因子平均 VIP：OPEC 0.589、标普500 1.102、美元指数 1.118、VIX 1.086；这是原算法结果，字段退化问题仍需解决。
- 原代码四模型测试 MAE 依次为 0.004771、0.004867、0.005821、0.007159，全部高于零收益率基线 0.004704。误差为插值后的对数差分单位，不是货币单位。
- 新确认一个运行问题：本次 Keras 3.14.0 下，四组共用 `EarlyStopping` 对象会保留上一组的 `best` 值。后三组均在 20 轮后停止且恢复各自第一轮权重，使模型对比不公平。已通过独立的最小回调测试复现，应改成每组独立回调后重跑。
- 实际可运行的环境含 TensorFlow 2.21.0，但与原 `requirements.txt` 不同；依赖版本及源代码校验信息已保存。

结论：代码可运行，但当前结果不足以支持“预测有效”或某模型稳定最优；GitHub 上传仍未执行。

## 你做了什么

研究对象是布伦特原油期货，工作分为两条互相关联的主线：一条通过 OHLC 变换、自编稳健 PLS 和 VIP 分析外部因子重要性；另一条通过 LSTM 与因果膨胀卷积模型比较三因子、四因子的预测表现。目录还保存了混合网络、单变量/多变量对比和价格水平预测等探索过程。

可确认的产出包括 6 个核心 Python 脚本、6 个历史 Python 脚本、多个预测与因子分析图表、模型权重与检查点、指标 JSON 和结题报告。存在输出文件只能证明保存过相关产物，不能证明它们都来自当前版本的代码。

报告是团队材料，现有目录无法可靠区分每位成员的具体代码贡献，因此以上总结是项目成果，不将全部团队工作归属于某一个人。

## 上传前需要处理的问题

### 1. 预测流程存在数据泄漏

四个核心预测脚本都先对全量特征与目标 `fit_transform`，再划分训练、验证和测试集。测试区间的极值进入了缩放器。应先按目标日期划分数据，只在训练范围拟合缩放器，再转换其他区间，重新训练并评估。

三因子 LSTM、四因子 TCN 和综合对比脚本还执行全日历重采样与时间插值。插值可能使用后续观测值补前面的缺失值，实时预测时这些值未必可用。应明确按交易日还是日历日建模，并采用只使用当时已知信息的处理规则。

### 2. OPEC 数据含义与处理需要核实

报告称 OPEC 为原油产量，代码用 `close` 字段。抽查 `欧佩克油.xlsx`，表头为 `Date/open/high/low/close`，开头两行四个数值均为 29309。这不足以断言它是市场价格；也可能是产量被复制为 OHLC 字段。需要补充原始来源、单位、频率、发布日期与字段转换过程。

VIP 代码针对 OHLC 区间做变换；若 OPEC 四列只是同一产量值，区间宽度为零，会被 epsilon 替换，产生常数或退化特征。这会影响因子间的可比性。在核实前，不能由该变量的 VIP 直接推导“供给因素较弱”的结论。

### 3. 报告表述超出现有验证证据

- 核心预测目标为对数差分，其 MAE/RMSE 不能当成货币单位的价格误差。
- 独立四因子 LSTM 使用共同观测日期并删除缺失值，其他核心预测脚本使用日历插值；不能直接将独立脚本图表当成相同条件下的对照。
- 报告描述 TCN 包含残差连接，但当前核心 TCN 与综合对比实现中未见残差相加。
- VIP 分析按全部可用年份执行，预测脚本手工指定三/四因子，没有展示仅用训练期选择因子的独立流程。若据全样本 VIP 选择特征，也需检查选择阶段的数据泄漏。
- 未发现与核心四模型实验完整对应的结构化结果记录、重复随机种子实验或显著性检验。不能只凭现有材料确认“显著优于”“可靠决策支撑”等强结论。
- 自编稳健 PLS/VIP 与参考论文的数学一致性，本次未验证。

### 4. 复现与结果溯源不足

`requirements.txt` 缺少 TensorFlow，却包含多项当前核心脚本未使用的环境包；安装与版本兼容性未验证。脚本依赖启动目录，模型文件也未统一配套保存特征缩放器、字段顺序、窗口、数据日期范围及运行配置。

`模型输出/oil_model_results.json` 保存的是单变量/全因子/VIP 因子下 LSTM、CNN 和 Ensemble 的历史结果，不能替代 LSTM/TCN 四模型实验。该文件中单变量 LSTM 的 RMSE 约为 4.3707，VIP 因子 LSTM 约为 5.6732；因此这些结果也不能用于支持“VIP 筛选在所有实验中更好”。部分检查点对应的完整训练入口尚未在现有代码中确认。

### 5. 隐私与上传范围

原结题报告含团队成员及导师的个人信息，包括学号、电话和邮箱，不适合直接公开上传。原始数据的再分发权限未确认。新增 `.gitignore` 默认排除原始报告、数据、模型输出、本地环境、编辑器配置、权重和密钥类文件，保留本地原件。

代码与选定文本文件的常见密钥模式扫描未发现匹配项；这不是完整安全审计。此次未逐张检查全部图表和文档嵌入对象，不保证所有二进制文件不含敏感内容。非环境项目文件中最大文件约 1.6 MB，当前主要顾虑不是项目文件体积。

## 已完成检查与改动

- 12 个 Python 文件均通过 AST 语法解析；此项不代表依赖齐全、程序能运行或结果正确。
- GitHub CLI 已确认登录；本目录尚未初始化 Git，尚未创建远程仓库，也没有上传文件。
- 仅新增 `README.md`、本检查文档和 `.gitignore`；原代码、依赖清单、报告、数据和图表保留不动。

## 建议下一步

优先核实 OPEC 数据定义，修正时间切分、归一化和缺失值处理，统一对照实验与基线，补齐运行环境并重新评估，再同步修订结论。如果只需要保存现状，也可以明确标为“存在已知问题的研究归档”后上传私有仓库，但不能把这次检查解释为“项目没有问题”。
